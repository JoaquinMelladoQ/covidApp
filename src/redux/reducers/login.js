const defaultState = {
  loading: false,
  valid: undefined,
};

const login = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state; 
  }
}; 

export default login;
